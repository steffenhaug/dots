# This is searched for in $HOME, so symlink
#  $HOME/.zshenv -> this file
#  $HOME/.config/zsh -> the zsh config directory
export ZDOTDIR=$HOME/.config/zsh
export XAUTHORITY=$HOME/.local/share/X/Xauthority
export XDG_CONFIG_HOME=$HOME/.config
