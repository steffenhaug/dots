" Sensible tab settings.
filetype plugin indent on
set ts=4 sw=4 expandtab

" Line numbers.
set number

" Use system clipboard by default, to easily
" copy/paste between VIM in multiplexed terminal.
set clipboard=unnamedplus
